import { cssRule, style } from 'typestyle'

export const container = style({ })

export const sections = style({
  display   : 'flex',
  margin    : '15px 0 0 0',
})

export default {
    container,
    sections,
}
